node_modules
.env
.DS_Store
Thumbs.db
npm-debug.log*
yarn-debug.log*
yarn-error.log*
data/*.tmp

# Keep deployed product images in Git so the current catalog does not break
# after the first GitHub -> Zeabur deployment.
!public/uploads/products/.gitkeep
